# harness-local state (plans/, session artifacts)
.zcode/

# audit output produced when this skill audits its own host repo
release-audit/

# python
__pycache__/
*.pyc

# os / editor
.DS_Store

# node (lighthouse runs via npx)
node_modules/
